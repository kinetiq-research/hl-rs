[package]
name = "perp-deploy-enable-growth-mode"
version = "0.1.0"
edition = "2024"

[dependencies]
hl-rs = { path = "../../" }

alloy = "1.1.1"
dotenv = "0.15.0"
tokio = { version = "1.48.0", features = ["macros"] }
